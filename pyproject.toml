[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "chaos-llm"
version = "0.1.0"
description = "Chaos Engineering for LLM APIs"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]

[tool.setuptools.packages.find]
where = ["src"]
